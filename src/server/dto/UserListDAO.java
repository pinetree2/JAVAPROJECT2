package server.dto;

import server.User;
import server.dto.Database;

import java.util.Vector;

public class UserListDAO {
    Database db = new Database();
    public static void userlistDAO(Vector<User> userlist) {

    }
}
